import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";

import { AddressField } from "./address-field";

export const onFieldEvent = action("onFieldEvent");

<Meta title="Components|Forms|AddressField" component={AddressField} />

<Preview withToolbar>
  <Story name="Address field">
    <AddressField
      elementId="address-field-1"
      label="Adresse"
      fieldEvent={onFieldEvent}
    />
  </Story>
</Preview>

### Required

Just add ´required` prop

<Story name="Address field required">
  <AddressField
    elementId="address-field-1"
    label="Adresse"
    required
    fieldEvent={onFieldEvent}
  />
</Story>

### Full width

<Story name="Address field full-width">
  <AddressField
    elementId="address-field-1"
    label="Adresse"
    fullWidth
    fieldEvent={onFieldEvent}
  />
</Story>

### Disabled

Just add `disabled`prop:

<Story name="Address field disabled">
  <AddressField
    elementId="address-field-1"
    label="Telefonnummer"
    disabled
    fieldEvent={onFieldEvent}
  />
</Story>

<Props of={AddressField} />
